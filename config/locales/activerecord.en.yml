en:
  activerecord:
    models:
      test: "Test"
      category: "Category"

    attributes:
      test:
        title: "Name"
        category: "Category"
      user:
        email: "email"
        password: "password"

    errors:
      messages:
        blank: "can't be blank"
        required: "must exist"